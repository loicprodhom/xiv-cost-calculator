import React from "react";
import { Grid } from "@mui/material";

const ItemInputComponent = (props) => {
    return (
        <Grid>This is the window to enter an Item's information</Grid>
    )
}

export default ItemInputComponent;